General:
  ConfigVersion: "5.9.0"
  ValidInstancePaths:
    - /tgs_instances

Database:
  DatabaseType: MariaDB
  ServerVersion: "10.11.0"
  # This connection string is overridden at runtime by the
  # Database__ConnectionString environment variable in docker-compose.yml.
  ConnectionString: "Server=mariadb;Port=3306;Database=tgs;Uid=ss13;Pwd=override_via_env;"

Kestrel:
  Endpoints:
    Http:
      Url: "http://0.0.0.0:5000"
